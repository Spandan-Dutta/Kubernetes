apiVersion: v1
kind: Service
metadata: 
  name: nginx-nodeport-svc
  labels:
    env: demo
spec:
  type: NodePort
  selector:
    app: nginx
    tier: frontend
  ports:
  - port: 80 # service port
    targetPort: 80 # container port
    nodePort: 30080 # node port
