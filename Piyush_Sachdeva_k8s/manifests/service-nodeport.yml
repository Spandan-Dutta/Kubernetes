apiVersion: v1
kind: Service
metadata: 
  name: nginx-nodeport-svc
  labels:
    env: demo
spec:
  type: NodePort
  selector:
    app: nginx
    tier: frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
