apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  labels:
    env: demo
    tier: frontend
spec:
  type: NodePort
  selector:
    env: demo
    tier: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001